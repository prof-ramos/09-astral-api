# Environment configuration
ENV_TYPE=dev

# API Authentication - Required for production
# Comma-separated list of allowed API keys
ALLOWED_API_KEYS=your_secret_api_key_1,your_secret_api_key_2

# GeoNames integration - Optional but recommended
# Sign up for free at: https://www.geonames.org/login
GEONAMES_USERNAME=your_geonames_username

# Development settings
# DEBUG=true  # Only used if not set via TOML config files